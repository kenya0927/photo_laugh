<h2 class="mb-4 text-center w-25 h4 bg-light d-inline-block">いいね一覧</h2>

<% @comments.each do |topic_comment| %>

<div>
<%= link_to topic_comment.comment, topic_path(topic_comment.topic) %>
<%= topic_comment.user.name %>
<%= topic_comment.created_at.strftime('%Y/%m/%d') %>
</div>


<% end %>